<UserControl x:Class="Cleaner.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="Segoe UI">
    <Border Style="{StaticResource Card}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,14">
                <TextBlock Text="История очисток" Style="{StaticResource PageTitle}" />
                <TextBlock x:Name="SummaryText" Foreground="{StaticResource Muted}" FontSize="13" Margin="0,5,0,0" />
            </StackPanel>
            <ListView Grid.Row="1" x:Name="HistoryList" BorderBrush="{StaticResource Line}" BorderThickness="1" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Date}" Width="170" />
                        <GridViewColumn Header="Категории" DisplayMemberBinding="{Binding Scope}" Width="330" />
                        <GridViewColumn Header="Файлов" DisplayMemberBinding="{Binding DeletedFiles}" Width="120" />
                        <GridViewColumn Header="Освобождено" DisplayMemberBinding="{Binding Reclaimed}" Width="170" />
                    </GridView>
                </ListView.View>
            </ListView>
            <Button Grid.Row="2" x:Name="ClearHistoryButton" Content="Очистить историю" Style="{StaticResource SecondaryButton}" HorizontalAlignment="Right" Margin="0,14,0,0" Click="ClearHistoryButton_Click" />
        </Grid>
    </Border>
</UserControl>
